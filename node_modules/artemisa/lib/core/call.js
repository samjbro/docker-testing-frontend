'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

// *************************
// ** HTTP Call Factories
// *************************

/**
 * Full call object can contain all this parameters
 * @param method
 * @param path
 * @param urlParams
 * @param body
 * @param token
 * requiresAuthentication: boolean
 */
var call = exports.call = function call(method, path, urlParams, body, token) {
  return _extends({
    method: method,
    path: path
  }, urlParams && { urlParams: urlParams }, body && { body: body }, token && { token: token });
};

var get = exports.get = function get(path) {
  return { method: 'GET', path: path };
};
var post = exports.post = function post(path, body) {
  return { method: 'POST', path: path, body: body };
};

// This should eventually become an ecmascript decorator function
// to use it like
//
//  @auth
//  fetchWeather(city) { ... }
var auth = exports.auth = function auth(aCall) {
  return _extends({}, aCall, { requiresAuthentication: true });
};